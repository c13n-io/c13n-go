package services

import (
	// Blank import in order to declare service proto dependency.
	// This is needed due to the fact that we do not include
	// the generated .pb.go file in the repository.
	_ "github.com/mwitkow/go-proto-validators"
)
